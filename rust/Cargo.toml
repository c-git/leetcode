[package]
name = "rust"
version = "0.1.0"
edition = "2021"
default-run = "name_converter"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
nom = "*"
regex = "*"
rstest = "0.12.0" #Old version used because or old version of rust on leetcode
